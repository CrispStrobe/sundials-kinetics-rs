[package]
name = "chem_core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
sundials = { path = "../sundials" }
symengine = { path = "../symengine" }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
